# Architecture

## Prototype (Current)
- Keep prototype changes in `index.html`.
- Avoid adding frameworks/build tooling unless approved via architecture issue.

## Production (Target)
- Blueprint-first iteration, migrate core/network-sensitive systems to C++ before release.